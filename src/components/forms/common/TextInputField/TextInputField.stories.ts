import type { StoryObj } from '@storybook/react';
import { TextInputField } from './TextInputField';

type Story = StoryObj<typeof TextInputField>;

export default { component: TextInputField };

export const Default: Story = {};
